const testVar = {}

function testFunc() {
  return "hi"
}


function superbowlWin(records){
  const win = records.find(record => record === "W");
  if (record === "W")
    return record.year;
  else 
    return undefined;
}